import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
//appcomponent howa esm el component
export class AppComponent {
  /*
  //title = 'ecom';
  //simple variable
  //optional : String
  public name: String = 'FormaLab'; //; optional

  //json object
  public book: any = {
    title: 'Learn Angular in 2 days',
    price: 0,
  };

  //type is optional : any[]

  //tableau
  public users = ['sirine', 'taher', 'nadhem', 'khawla', 'amel', 'yosra'];

  deleteUser(theuser): void {
    console.log(theuser);

    this.users.splice(this.users.indexOf(theuser), 1);
  }
*/
}
